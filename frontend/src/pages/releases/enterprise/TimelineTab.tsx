import type { ReleaseResponse } from '../../../types/release';

export function TimelineTab({ release: _release }: { release: ReleaseResponse }) {
  return <div>Timeline — coming soon (Task 30)</div>;
}
