import type { ReleaseResponse } from '../../../types/release';

export function MembersTab({ release: _release }: { release: ReleaseResponse }) {
  return <div>Members tab — coming soon (Task 27)</div>;
}
