import type { ReleaseResponse } from '../../../types/release';

export function ScopeRollupTab({ release: _release }: { release: ReleaseResponse }) {
  return <div>Scope rollup — coming soon (Task 29)</div>;
}
