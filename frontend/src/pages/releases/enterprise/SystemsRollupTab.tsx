import type { ReleaseResponse } from '../../../types/release';

export function SystemsRollupTab({ release: _release }: { release: ReleaseResponse }) {
  return <div>Systems Impacted rollup — coming soon (Task 28)</div>;
}
